# getdir, a cross-platform library for... getting file names!
## uses:
none
## how to use:
override the `getdir.dir` with whatever directory you wanna look at.

call `getdir.getfilenames`.

your filenames are now stored at `getdir.filenames`.
